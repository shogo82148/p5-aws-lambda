name: Test

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        perl:
          - "5.32"
          - "5.30"
          - "5.28"
          - "5.26"
    name: Perl ${{ matrix.perl }}
    steps:
      - uses: actions/checkout@v2
      - name: Setup perl
        uses: shogo82148/actions-setup-perl@v1
        with:
          perl-version: ${{ matrix.perl }}
      - name: Install Dependencies
        run: |
          cpm install Minilla Software::License::MIT Test::CPAN::Meta Test::Pod Test::MinimumVersion::Fast Mojolicious Dancer2
          cpm install --with-develop .
      - name: Test
        run: minil test --all
